# Presence of this file at the repo root puts the root on sys.path during
# collection (pytest's default "prepend" import mode), so tests can `import exp`
# without setting PYTHONPATH or installing the package. Intentionally empty.
